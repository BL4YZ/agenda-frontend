import ForgotPasswordScreen from '@/components/auth/ForgotPasswordScreen';

export const metadata = { title: 'Recuperar contraseña — MiAgenda' };

export default function ForgotPasswordPage() {
  return <ForgotPasswordScreen />;
}
